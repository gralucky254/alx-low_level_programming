#include "main.h"
/**
 * add - adds two integers and returns the result
 * @i: int to be added to n
 * @n: int to be added to i
 * Return: Always 0.
 */
int add(int i, int n)
{
	int j;

	j = i + n;
	return (j);
}
